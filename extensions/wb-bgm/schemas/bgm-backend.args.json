{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "forgeax://plugin/wb-bgm/bgm-backend.args",
  "title": "bgm:backend args (packaged library compatibility; read-only)",
  "type": "object",
  "properties": {
    "endpoint": { "type": "string", "const": "FindAssetMeta", "description": "Compatibility operation used by the SPA." },
    "payload": { "type": "object", "description": "Local query and pagination. asset_type, if present, must be 3 or 7." }
  },
  "required": ["endpoint"]
}
