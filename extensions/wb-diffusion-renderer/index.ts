/**
 * Stable public entry for Studio and other hosts.
 *
 * Internal implementation files remain under `src/`; consumers should import
 * this module instead of depending on that layout.
 */
export * from './src/index';
