{
  "id": "list_explode",
  "label": "字符串列表爆炸",
  "description": "输入一个 JSON 数组字符串，将其中每个元素拆分为独立的动态输出端口，端口数量与列表长度一致。",
  "description-en": "Parse a JSON array string and explode each element into an individual dynamic output port.",
  "icon": "一个橙色列表方块向右爆炸散射出多条绿色箭头线，每条线末端连接一个字符串标签，扁平插画风格，简洁小图标，主色2种。",
  "version": "1.0.0",
  "tags": ["string", "list", "array", "explode", "split", "transform"],
  "tag_labels": ["字符串", "列表", "数组", "爆炸", "拆分", "转换"],
  "inputs": [
    {
      "name": "list",
      "type": "string",
      "label": "输入字符串",
      "description": "形如 [...] 的 JSON 数组字符串，每个元素会输出为独立的端口",
      "description-en": "A JSON array string like [...]; each element is output as an individual port."
    }
  ],
  "outputs": [],
  "dynamicOutputs": {
    "prefix": "item_",
    "labelTemplate": "[$i]",
    "minCount": 2,
    "type": "any"
  }
}
