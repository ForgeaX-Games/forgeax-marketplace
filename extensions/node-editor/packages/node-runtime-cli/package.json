{
  "name": "@forgeax/node-runtime-cli",
  "version": "0.1.0",
  "description": "forgeax CLI — every Layer 2 editing API exposed as a shell subcommand for AI-native automation",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "forgeax": "./dist/bin.js"
  },
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "prepare": "tsc -b"
  },
  "dependencies": {
    "@forgeax/node-runtime": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
