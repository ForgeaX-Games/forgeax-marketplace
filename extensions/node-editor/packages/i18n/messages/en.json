{
  "kernel": {
    "execute.started": "Pipeline {pipelineId} started",
    "execute.completed": "Pipeline {pipelineId} completed in {duration}ms",
    "execute.aborted": "Pipeline {pipelineId} aborted"
  }
}
