node_modules/
# This repository is a pnpm workspace; a local `bun install` must not make
# the nested submodule dirty or bubble into forgeax-marketplace/Studio.
bun.lock
apps/*/bun.lock
*.tsbuildinfo
*.log

# Build output — committed later (Phase 4) so the host can serve without a build
# step; ignored for now during the consolidation milestone.
dist/
vendor/dist/
release/

.DS_Store

# Build/runtime junk
**/node-compile-cache/
core.*
**/.review-evidence/
**/.scratch-repro/
**/.scratch-parity/
apps/wb-scene-generator/batteries/groups/test/
