{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lowpoly:playground-snapshot args",
  "description": "Save a screenshot of the playground viewport. UI-only (exposedToAI: false).",
  "type": "object",
  "additionalProperties": false,
  "required": ["gameSlug", "slug", "imageBase64"],
  "properties": {
    "gameSlug": { "type": "string" },
    "slug": { "type": "string" },
    "imageBase64": {
      "type": "string",
      "description": "PNG bytes, base64-encoded. Server decodes and writes under previews/<timestamp>.png."
    },
    "label": { "type": "string", "maxLength": 80 }
  }
}
