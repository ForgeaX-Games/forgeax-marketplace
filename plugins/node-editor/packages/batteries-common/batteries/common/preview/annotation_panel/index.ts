/**
 * annotationPanel 电池执行函数
 *
 * 行为：该电池仅用于在编辑器画布上创建自由文本注释，不参与数据管线执行。
 *
 * 运行约定：函数名小写字母开头，入参/出参均为 Record<string, unknown>
 */
export function annotationPanel(_input: Record<string, unknown>): Record<string, unknown> {
  return {}
}
