{
  "id": "grid_json_to_size",
  "label": "网格 JSON 转尺寸",
  "label-en": "Grid JSON to Size",
  "description": "解析二维列表 JSON 字符串，按每格 16px 计算其对应灰度图的宽度与高度（宽=列数×16，高=行数×16）。",
  "description-en": "Parse a 2D-list JSON string and compute the matching grayscale image width/height at 16px per cell (width = cols×16, height = rows×16).",
  "icon": "一张方格网格旁标注着宽高尺寸箭头，扁平插画风格，适合做电池图标，小尺寸可辨、背景简洁、主色一至两种。",
  "version": "1.0.0",
  "tags": ["data", "grid", "size", "json"],
  "tag_labels": ["数据", "网格", "尺寸", "JSON"],
  "inputs": [
    {
      "name": "json",
      "type": "string",
      "label": "JSON 字符串",
      "description": "二维列表格式的 JSON 字符串，例如 [[0,1],[1,0]]。",
      "description-en": "A JSON string in 2D-list format, e.g. [[0,1],[1,0]].",
      "default": ""
    }
  ],
  "outputs": [
    {
      "name": "width",
      "type": "number",
      "label": "宽度",
      "description": "列数 × 16（像素）。",
      "description-en": "Number of columns × 16 (pixels)."
    },
    {
      "name": "height",
      "type": "number",
      "label": "高度",
      "description": "行数 × 16（像素）。",
      "description-en": "Number of rows × 16 (pixels)."
    }
  ]
}
