{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "reel:generate-storyboard args",
  "description": "Split a scene (or the whole episode) into multiple shots and write scene.shots[] with timeline placeholders. The workbench consumes the queue, runs its director storyboard engine in-browser, and lays the shots out on the timeline for preview. The workbench must be open. This is the FIRST step of per-node production: do it before generating per-shot keyframes/videos.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "scope": {
      "type": "string",
      "enum": ["scene", "all"],
      "description": "'scene' (default): split only the node given by sceneId (focused, cheap). 'all': run the batch engine over the whole episode (cross-scene consistency loopback)."
    },
    "sceneId": {
      "type": "string",
      "description": "Required when scope='scene': the node (key in scenario.scenes) to storyboard."
    },
    "scenarioId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{1,64}$",
      "description": "Optional target scenario id. Defaults to the workbench's active scenario."
    }
  }
}
