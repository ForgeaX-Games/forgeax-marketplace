{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lowpoly:vibe-edit args",
  "description": "LLM proposes patch ops (SourceOp[]) against current source.json. Server applies ops, zod-revalidates, then writes.",
  "type": "object",
  "additionalProperties": false,
  "required": ["gameSlug", "slug", "instruction"],
  "properties": {
    "gameSlug": { "type": "string" },
    "slug": { "type": "string" },
    "instruction": { "type": "string", "minLength": 1, "maxLength": 4000 },
    "model": { "type": "string" },
    "dryRun": { "type": "boolean", "default": false, "description": "When true, return ops without writing." }
  }
}
