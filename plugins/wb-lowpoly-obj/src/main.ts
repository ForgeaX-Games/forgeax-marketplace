/**
 * wb-lowpoly-obj — iframe entry stub.
 *
 * v0.1.0 scaffold: no UI wiring yet. Subsequent commits add:
 *   - core/Engine.ts          (three.js renderer + camera controls)
 *   - core/HumanoidSkeleton.ts (humanoid-standard-v1 17-bone rig)
 *   - core/PartBuilder.ts     (LowPolySource.parts → THREE.Mesh)
 *   - pipelines/{sculpt,vibe,anim,playground,bake}/
 *   - ui/{Viewport,PartList,PartInspector,VibeBox,...}
 */

console.log('[wb-lowpoly-obj] scaffold loaded; pipelines pending')

export {}
